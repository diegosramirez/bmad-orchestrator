/**
 * English copy when the Digistore / BMAD target repository custom field is empty.
 * Shared by Forge UI and resolvers.
 */
export const TARGET_REPO_REQUIRED_MESSAGE_EPIC =
  'Select the Digistore repository on this epic (Repository field) first, then try again.';

export const TARGET_REPO_REQUIRED_MESSAGE_ISSUE =
  'Select the Digistore repository on this issue (Repository field) first, then try again.';
