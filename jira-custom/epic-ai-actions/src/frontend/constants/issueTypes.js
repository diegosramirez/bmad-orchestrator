/**
 * Lowercased issuetype.name values treated as Epic for the BMAD Epic panel.
 */
export const EPIC_TYPE_NAMES = new Set(['epic']);

/**
 * Lowercased issuetype.name values treated as Story for the AI Comments panel.
 */
export const STORY_TYPE_NAMES = new Set([
  'story',
  'user story',
  'historia de usuario',
  'technical story',
]);
